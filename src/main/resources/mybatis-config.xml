<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="org.mariadb.jdbc.Driver"/>
                <property name="url" value="jdbc:mariadb://localhost:3306/grade_buddy"/>
                <property name="username" value="buddy_user"/>
                <property name="password" value="buddy_password_0317"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <!-- 매퍼 파일 위치 설정 -->
        <mapper resource="mappers/AssignmentMapper.xml"/>
        <mapper resource="mappers/FeedbackMapper.xml"/>
        <mapper resource="mappers/SubmissionMapper.xml"/>
        <mapper resource="mappers/UserMapper.xml"/>
    </mappers>
</configuration>